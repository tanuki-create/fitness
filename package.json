{
  "name": "fitness-ai-monorepo",
  "private": true,
  "workspaces": [
    "web",
    "supabase/functions/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web"
  },
  "devDependencies": {
    "supabase": "^2.30.4"
  }
}
